using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WebPagesManager : MonoBehaviour
{

    WebPagesManager()
    {
        
    }
    
    // Start is called before the first frame update
    void Start()
    {
        //webLibrary.webPages.Add(new WebBrowserLibrary.WebPage { pageTitle = "temp", pageURL = "temp.com", pageIcon = null, pageContent = null, isModContent = false, modHelper = false });

    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    public string getProfileData(string profileName)
    {
        return "Profile Data";
    }
}
